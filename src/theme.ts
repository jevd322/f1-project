// theme.ts intentionally kept minimal after removing MUI.
// If you want a shared theme object for Tailwind/shadcn components, place it here.

const theme: Record<string, unknown> = {};

export default theme;
